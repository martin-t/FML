function f() ->
 let d = 5;
 let a = 1;
 let b = 2;
 let c = 3;
 print("~ ~ ~ ~\n", a, b, c, d);
f()

// TODO need errors without line numbers
// > thread 'main' panicked at 'Interpreter error: Error evaluating get global #14:
// > 
// > Caused by:
// >     No such global `d`.', src/main.rs:188:14
// > note: run with `RUST_BACKTRACE=1` environment variable to display a backtrace
